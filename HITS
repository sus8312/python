col="HIT"
class AIML:
    dept="ARTIFICAL"
    def set_dim(self,a,b,c,d):
        self.m1=a 
        self.m2=b 
        self.m3=c 
        self.name=d 
    def display(self):
        print("1.Name:",self.name)
        print("2.Marks:",self.m1+self.m2+self.m3)
        print("3.Dept:",CSE.dept)
        print("4.College:",col)
class CSE:
    dept="ARTIFICAL"
    def set_dim(self,a,b,c,d):
        self.m1=a 
        self.m2=b 
        self.m3=c 
        self.name=d 
    def display(self):
        print("1.Name:",self.name)
        print("2.Marks:",self.m1+self.m2+self.m3)
        print("3.Dept:",CSE.dept)
        print("4.College:",col)
        
S1=AIML()
S2=AIML()
C1=CSE()
C2=CSE()

S1.set_dim(40,50,60,"sushmitha")
S2.set_dim(70,50,40,"sujith")
S1.display()
S2.display()

C1.set_dim(40,50,60,"sai")
C2.set_dim(20,30,40,"mouni")
C1.display()
C2.display()

